# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Docker infrastructure secrets — never commit. Copy .env.example or
# .env.template to .env instead.
.env
.env.*.local

# Local pg_dump / volume backup output (see docs/infrastructure.md)
backups/

# Playwright test output (not source artifacts — regenerated on every run)
test-results/
playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
